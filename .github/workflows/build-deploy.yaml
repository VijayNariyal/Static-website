name: "code build and deploy"

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Dockerversion
        run: docker --version
        
      - name: Docker Build
        run: docker build -t staticwebsite .

      - name: Docker Login
        uses: docker/login-action@v4.1.0
        with:
          registry:
          username: VijayNariyal
          password: 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

      

